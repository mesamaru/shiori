# Discordサーバー × AI連携プロジェクト 計画書

## 1. 目的

- Discordサーバー内のプロジェクトに関する会話をリアルタイムで抽出・保存する
- 会話内容をもとに、タスクの進行度を自動更新する
- 最新情報をもとに、サーバーメンバーへお知らせ（通知）を自動送信する
- AI（Claude API）の利用は**低頻度・差分ベース**に抑え、コストを最小化する

---

## 2. 全体アーキテクチャ

```
Discordメッセージ
   ↓
① Bot（常時稼働）: メッセージ監視・保存
   - 新規メッセージ → DB保存
   - 編集メッセージ → 更新フラグを立てて保存
   - 削除メッセージ → 削除記録（必要に応じて）
   ↓
② ルールベース一次判定（キーワード検知）
   - 例：「完了」「done」「ブロッカー」「進捗」など
   - AI判定が必要なものだけを「差分候補」として蓄積
   ↓
③ ユーザーからのアクション時のみ AIバッチ処理
   - 「前回AIに渡した時点」からの差分（新規＋編集分）のみ抽出
   - 差分をコンテキストとしてClaude APIに渡す
   - 差分が無ければAI呼び出し自体をスキップ
   ↓
④ タスクDB更新
   ↓
⑤ 変更があればDiscordへ通知（Bot送信 / Webhook）
```

### 設計のポイント（コスト最小化の要）

- Botの常時監視・保存処理自体はAPIコストゼロ（Discord側で完結）
- AI呼び出しは「ユーザーがアクションを起こした時」だけ発生
- 「最後にAIへ渡したメッセージID／タイムスタンプ」をカーソルとして管理し、差分のみを都度読み込む
- 単純な判定（キーワード一致など）はルールベースで処理し、AIコールを使わない
- 曖昧な判断が必要なものだけ低頻度バッチでAIに回す

---

## 3. インフラ構成

| 役割 | 技術 | 備考 |
|---|---|---|
| Bot実行環境 | Pterodactyl（node-js-generic egg） | 常時稼働、Discordイベント監視 |
| 永続データ保存 | **MariaDB**（Proxmox上に構築） | メッセージログ、タスク状態、変更履歴など構造化データ |
| 軽量キャッシュ／カーソル管理 | **Redis**（Proxmox上に構築） | 「チャンネルごとの最終読み込みメッセージID」、差分候補の一時キューなど |
| AI | Claude API（従量課金・低頻度バッチ運用） | 差分ベースで呼び出し、コストを最小化 |
| 通知 | Discord Bot メッセージ送信 / Webhook | 無料 |

### MariaDB（永続データ）想定テーブル

- `messages`：id, channel_id, author, content, created_at, edited_at, is_deleted
- `tasks`：id, project_id, title, status, last_updated
- `task_history`：task_id, changed_by, change_summary, changed_at

### Redis（一時データ）想定キー

- `cursor:{channel_id}` → 最後にAIへ渡したmessage_id
- `pending_diff:{channel_id}` → 差分候補メッセージの一時キュー（AIバッチ処理前）

### ネットワーク構成

- MariaDB・RedisはProxmox上のLXC/VMとして構築（コンテナ分離で管理しやすくする）
- Botから同一LAN内のプライベートIPで接続（外部公開は不要）

---

## 4. 確定事項（詳細仕様）

### タスク管理単位
- **パターンC（新規登録＋既存タスク更新の両方）**
  - 新規タスク検出：会話から「〇〇やる」等の発言を拾い、DBに存在しないタスクを新規登録
  - 進捗更新検出：既存タスクと紐づけて「完了」「進行中」「ブロッカー」等のステータス変化を検出
  - 補足：AIに新規/更新を判定させるには「現在のタスク一覧（特にアクティブなもの）」も併せて渡す必要あり。タスク増加に伴うトークン増加を避けるため、完了済みタスクは判定対象から除外する運用を検討

### ルールベース一次判定
- **言語**：日本語中心（表記ゆれ対応の正規表現設計が必要）
- **対象チャンネル**：固定にせず、運用側が選択できる方式とする
  - 対象チャンネルIDのリストを設定ファイル or DBで管理し、後から追加・変更可能にする

### 通知
- **通知先**：チャンネル投稿とDMの併用
  - 全体共有すべき情報（新規タスク登録、大きな進捗変化）→ チャンネル投稿
  - 個人に紐づく情報（担当タスクの完了通知、確認依頼など）→ DM

### AIバッチ処理の実行タイミング
- **手動実行のみ**（自分が指示した時だけAIを起動）
  - 定期実行なし。ユーザーアクション起点の設計方針と完全に一致し、無駄なAPI呼び出しが発生しない

---

## 5. 残タスク（実装前の確認事項）

- [ ] Discord Developer Portalでの `Message Content Intent` 権限有効化（未確認であれば要対応）

---

## 6. 次のアクション候補

1. MariaDBのテーブル設計（スキーマ確定）
2. Botの基本コード作成（メッセージ受信 → キーワード判定 → DB保存）
3. Redisによるカーソル管理ロジックの実装
4. AIバッチ処理（差分抽出 → Claude API呼び出し → タスクDB更新）の実装
5. 通知ロジックの実装

---

*作成日: 2026-08-24*
