# Cowork向け実装依頼書（Discord-to-AIプロジェクト）

このフォルダには以下のファイルが既に配置されています。作業前に必ず目を通してください。

- `discord-ai-project-plan.md` … 全体の要件・アーキテクチャ・確定仕様
- `db/schema.sql` … MariaDBのテーブル定義（IF NOT EXISTS形式）
- `db/db-init.js` … Bot起動時にスキーマを自動適用する初期化スクリプト

---

## 実現したいこと（全体像）

Discordサーバー内のプロジェクトに関する会話を常時収集し、以下を実現する。

1. タスクの新規登録・進捗更新を自動で行う
2. 最新情報をもとにメンバーへ通知（チャンネル投稿＋DM）する
3. AI（Claude API）の呼び出しは**自分が指示した時のみ**発生させ、コストを最小化する

---

## 実装してほしい範囲（フェーズ分け）

一度に全部ではなく、**フェーズ順に段階的に**実装してほしい。各フェーズごとに動作確認してから次に進める。

### フェーズ1: Bot土台（常時稼働・保存）
- discord.js でBotを起動し、指定チャンネル（`watched_channels`テーブルで管理）のメッセージを監視
- 新規メッセージ・編集メッセージ・削除を `messages` テーブルに保存
- 日本語のキーワード（完了/done/ブロッカー/進捗 など）でルールベース一次判定を行い、`is_rule_flagged` を立てる
- `db/db-init.js` を使ってBot起動時にスキーマを自動適用する
- Pterodactyl の node-js-generic egg で動く構成（`package.json`、`.env`雛形を含む）にする

### フェーズ2: 差分管理（Redis）
- Redisに「チャンネルごとの最終読み込みメッセージID」をカーソルとして保存
- 差分候補（`is_rule_flagged=TRUE` かつ未処理）をRedisの一時キューに積む

### フェーズ3: AI手動バッチ処理
- 自分が明示的にコマンド等で指示した時のみ、Claude APIを呼び出す
- 差分メッセージ＋現在のアクティブなタスク一覧・プロジェクト一覧をAIに渡し、以下を判定させる
  - 新規タスクの検出（`tasks`テーブルへの登録）
  - 既存タスクの進捗更新（`task_history`へ記録）
  - プロジェクトの判定（1チャンネルに複数プロジェクトが混在、複数チャンネルにまたがる可能性を考慮）
  - 担当者の判定（本文・メンションから読み取る。判断できない場合は `pending_clarifications` に登録）

### フェーズ4: 確認フロー
- `pending_clarifications` が作成されたら、該当チャンネルにBotが確認メッセージを投稿
- ユーザーがそのメッセージへ**返信（reply）**した内容を検知し、`resolved_answer` に保存、`status='resolved'` に更新
- 次回のAI手動実行時に、解決済みの確認事項を差分として取り込む

### フェーズ5: 通知
- タスクの新規登録・進捗更新があれば通知を送る
  - 全体共有すべき情報 → 該当チャンネルへ投稿
  - 個人に紐づく情報（担当タスクの完了通知など） → 担当者へDM
- 同じ変更に対する重複通知を防ぐため `notifications` テーブルを参照する

---

## 進め方の指示

- 上記フェーズ1から順番に実装してほしい。フェーズ1が完了し動作確認できるまで、フェーズ2以降には着手しないでほしい
- 各フェーズの実装ファイルは `Discord-to-AI` フォルダ内に直接作成・配置してほしい
- `discord-ai-project-plan.md` に記載の確定仕様（言語：日本語中心、通知先：チャンネル＋DM併用、AIバッチは手動実行のみ、等）と矛盾する実装をしないこと
- 不明点があれば、実装を進める前にこちらに確認すること

---

*このファイルはCoworkへの依頼内容を正確に伝えるために作成された指示書です。*
